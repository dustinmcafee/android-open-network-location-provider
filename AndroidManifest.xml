<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.dustinmcafee.nlp">

    <uses-permission android:name="android.permission.INSTALL_LOCATION_PROVIDER"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    <uses-permission android:name="android.permission.UPDATE_APP_OPS_STATS"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:label="@string/app_label"
        android:allowBackup="false"
        android:directBootAware="true">

        <!--
          The intent-filter action must match what your AOSP tree's
          LocationManagerService binds to for the NETWORK_PROVIDER.
          Android 14 (API 34) uses v3; older versions may use v2.
          See docs/INTEGRATION.md for details.
        -->
        <service
            android:name=".NetworkLocationService"
            android:exported="true"
            android:permission="android.permission.INSTALL_LOCATION_PROVIDER"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="com.android.location.service.v3.NetworkLocationProvider"/>
                <category android:name="android.location.provider.LOCATION_PROVIDER"/>
            </intent-filter>
        </service>

    </application>
</manifest>
